# 🏗️ HuyEngine - Система автоматизации сборки v2.0

Современная система автоматизации сборки с интерактивным меню и поддержкой различных IDE.

## ✨ Основные возможности

- 🎮 **Интерактивное меню** - удобное управление без запоминания команд
- 🛠️ **Поддержка IDE** - Visual Studio, CLion, VSCode, Command Line
- ⚡ **Быстрая сборка** - Ninja, MSBuild, Unix Makefiles
- 📁 **Умные папки** - отдельные папки для каждой конфигурации
- 🔍 **Автообнаружение** - автоматический поиск CMake, Ninja, Visual Studio
- 🎨 **Цветной вывод** - наглядное отображение процесса
- ⚙️ **Гибкая настройка** - пользовательская конфигурация
- 📚 **Документация** - полное руководство на русском языке

## 🚀 Быстрый старт

### Интерактивный режим (самый простой)

```batch
build.bat
```

### Командная строка

```batch
# Полная пересборка
build.bat rebuild

# Сборка
build.bat build

# Запуск
build.bat run
```

## 📖 Документация

| Документ | Описание |
|----------|----------|
| [BUILD_QUICKSTART.md](../BUILD_QUICKSTART.md) | ⚡ Быстрый старт - начните здесь! |
| [BUILD_SYSTEM_README.md](BUILD_SYSTEM_README.md) | 📘 Полное руководство по системе |
| [BUILD_EXAMPLES.md](BUILD_EXAMPLES.md) | 💡 Примеры использования для разных сценариев |
| [BUILD_FAQ.md](BUILD_FAQ.md) | ❓ Ответы на частые вопросы |

## 🎯 Популярные сценарии

### Первая сборка проекта

```batch
build.bat
# Выберите: Сборка → Rebuild
```

### Для разработчиков Visual Studio

```batch
build.bat generate --ide vs --build-system msbuild
start vs-build\HuyEngine.sln
```

### Для разработчиков CLion

```batch
build.bat -i clion -c debug generate
# Откройте проект в CLion
```

### Быстрая сборка с Ninja

```batch
build.bat -b ninja -c release rebuild
```

### Форматирование кода

```batch
build.bat format
```

### Очистка всех артефактов

```batch
build.bat clean-all
```

## 📋 Требования

- **Python 3.6+** - для запуска скриптов
- **CMake 3.15+** - система сборки
- **Компилятор C++**:
  - Visual Studio 2019/2022 (Windows)
  - GCC/Clang (Linux/macOS)
- **Ninja** (опционально, рекомендуется для скорости)
- **clang-format** (опционально, для форматирования)

### Быстрая установка зависимостей (Windows)

```batch
choco install python cmake ninja
```

## 🎮 Интерактивное меню

```
======================================================================
                HuyEngine - Build Automation Menu
======================================================================

Текущая конфигурация:
  IDE:             Manual (Command Line)
  Система сборки:  Ninja
  Конфигурация:    Debug
  Платформа:       x64
  Папка сборки:    build

ГЛАВНОЕ МЕНЮ

  1. Конфигурация    - Настройка IDE, системы сборки, конфигурации
  2. Сборка          - Generate, Build, Rebuild, Clean, Run
  3. Инструменты     - Clang-Format, обновление путей
  0. Выход

Выберите пункт меню:
```

## ⌨️ Командная строка

### Основные команды

```batch
build.bat [действие] [опции]
```

**Действия:**
- `generate` - генерация проекта
- `build` - сборка
- `rebuild` - пересборка (clean + generate + build)
- `clean` - очистка текущей папки
- `clean-all` - очистка всех папок
- `run` - запуск приложения
- `format` - форматирование кода
- `menu` - интерактивное меню (по умолчанию)

**Опции:**
- `-i, --ide` - IDE (vs, clion, vscode, manual)
- `-b, --build-system` - система сборки (ninja, msbuild, make)
- `-c, --config` - конфигурация (debug, release, relwithdebinfo, minsizerel)
- `-p, --platform` - платформа (x64, win32, arm64)
- `--verbose` - подробный вывод
- `--no-clean` - не очищать перед сборкой

### Примеры

```batch
# Генерация для CLion Debug
build.bat generate --ide clion --config debug

# Сборка с Ninja Release
build.bat build --build-system ninja --config release

# Короткая форма
build.bat -i clion -c debug generate
build.bat -b ninja -c release build
```

## 📁 Структура папок

```
HuyEngine/
├── build.bat                     # Быстрый запуск из корня
├── BUILD_QUICKSTART.md           # Быстрый старт
├── Automation/
│   ├── automation_new.py         # Главный скрипт
│   ├── build_config.py           # Конфигурация
│   ├── build_actions.py          # Действия сборки
│   ├── build_menu.py             # Интерактивное меню
│   ├── build_new.bat             # Launcher для Windows
│   ├── user_config.template.py   # Шаблон пользовательской конфигурации
│   ├── BUILD_SYSTEM_README.md    # Полная документация
│   ├── BUILD_EXAMPLES.md         # Примеры использования
│   └── BUILD_FAQ.md              # Часто задаваемые вопросы
└── [папки сборки создаются автоматически]
    ├── build/                    # Ninja/Manual
    ├── vs-build/                 # Visual Studio
    ├── cmake-build-debug/        # CLion Debug
    └── cmake-build-release/      # CLion Release
```

## ⚙️ Пользовательская конфигурация

Создайте `Automation/user_config.py` для настройки под себя:

```python
# Скопируйте шаблон
copy Automation\user_config.template.py Automation\user_config.py

# Отредактируйте настройки
DEFAULT_IDE = "vs"
DEFAULT_BUILD_SYSTEM = "msbuild"
DEFAULT_CONFIGURATION = "release"

# Пользовательские пути (если нужно)
USER_CMAKE_PATH = r"D:\Tools\CMake\bin\cmake.exe"
USER_NINJA_PATH = r"D:\Tools\Ninja\ninja.exe"

# Дополнительные CMake флаги
CUSTOM_CMAKE_FLAGS = ["-DENABLE_TESTING=ON"]
```

## 🔍 Автоматическое обнаружение

Система автоматически находит:
- ✅ CMake (Program Files, chocolatey, PATH)
- ✅ Ninja (chocolatey, Program Files, PATH)
- ✅ Visual Studio 2019/2022 (Community, Professional, Enterprise)
- ✅ MSVC компиляторы и vcvarsall.bat

При запуске отображается статус:
```
Статус инструментов:
  CMake:  ✓ Найден
  Ninja:  ✓ Найден
  MSVC:   ✓ Найден
```

## 🆚 Сравнение со старой системой

| Функция | Старая | Новая |
|---------|--------|-------|
| Интерактивное меню | ❌ | ✅ |
| Поддержка IDE | ❌ | ✅ |
| Выбор системы сборки | Только Ninja | Ninja, MSBuild, Make |
| Отдельные папки | ❌ | ✅ |
| Автообнаружение | ❌ | ✅ |
| Цветной вывод | ❌ | ✅ |
| Документация | Минимальная | Полная на русском |
| Настройка | Править код | user_config.py |

## 🐛 Решение проблем

### Инструменты не найдены

```batch
# Установите через chocolatey
choco install cmake ninja

# Или создайте user_config.py с путями
```

### Ошибки компиляции

```batch
# Полная пересборка
build.bat clean-all
build.bat rebuild

# С подробным выводом
build.bat --verbose build
```

### Python не найден

```batch
# Установите Python
choco install python

# Убедитесь что Python в PATH
python --version
```

Больше решений в [BUILD_FAQ.md](Automation/BUILD_FAQ.md).

## 🤝 Для разработчиков

### Добавление нового действия

1. Добавьте функцию в `build_actions.py`
2. Добавьте вызов в `build_menu.py`
3. Добавьте в argparse в `automation_new.py`

### Тестирование

```batch
# Проверка help
build.bat --help

# Тестирование команды
build.bat generate --verbose

# Тестирование меню
build.bat
```

## 📜 Лицензия

Этот проект является частью HuyEngine.

## 🎉 Быстрые ссылки

| Что нужно | Команда |
|-----------|---------|
| 🚀 Начать работу | `build.bat` |
| 📖 Документация | [BUILD_QUICKSTART.md](../BUILD_QUICKSTART.md) |
| 💡 Примеры | [BUILD_EXAMPLES.md](Automation/BUILD_EXAMPLES.md) |
| ❓ Вопросы | [BUILD_FAQ.md](Automation/BUILD_FAQ.md) |
| ⚙️ Настройка | `Automation/user_config.template.py` |
| 🆘 Помощь | `build.bat --help` |

## 📝 История версий

### v2.0 (2025-01-13)
- ✨ Интерактивное меню
- ✨ Поддержка Visual Studio, CLion, VSCode
- ✨ Выбор системы сборки (Ninja, MSBuild, Make)
- ✨ Отдельные папки для конфигураций
- ✨ Автоматическое обнаружение инструментов
- ✨ Пользовательская конфигурация
- ✨ Полная документация на русском
- ✨ Цветной вывод

### v1.0 (ранее)
- Базовая автоматизация с Ninja
- Поддержка командной строки

---

**Начните прямо сейчас:**

```batch
cd C:\DEVOPS\HuyEngine
build.bat
```

**Приятной работы!** 🎯

