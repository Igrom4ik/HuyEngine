# HuyEngine - Инструкция по сборке

## ✅ Успешная установка системы сборки

Система автоматизации сборки успешно установлена и готова к использованию!

## 🚀 Быстрый старт

### Запуск в PowerShell

```powershell
# В PowerShell используйте .\ перед именем bat-файла
.\build.bat
```

### Запуск в CMD

```cmd
# В CMD можно запускать напрямую
build.bat
```

### Запуск Python напрямую

```powershell
python Automation\automation_new.py
```

## 📋 Доступные команды

### Интерактивное меню

```powershell
.\build.bat
# или
python Automation\automation_new.py
```

### Командная строка

```powershell
# Генерация проекта
python Automation\automation_new.py generate --ide clion --config Debug

# Сборка проекта
python Automation\automation_new.py build --build-system ninja --config Release

# Пересборка проекта
python Automation\automation_new.py rebuild

# Очистка текущей папки сборки
python Automation\automation_new.py clean

# Очистка всех папок сборки
python Automation\automation_new.py clean-all

# Запуск исполняемого файла
python Automation\automation_new.py run

# Форматирование кода
python Automation\automation_new.py format
```

### Быстрые команды

```powershell
# Генерация + Сборка для CLion Debug
python Automation\automation_new.py -i clion -c debug generate
python Automation\automation_new.py build

# Сборка Release с Ninja
python Automation\automation_new.py -b ninja -c release build
```

## 🎯 Системные требования

- Python 3.6+ ✅
- CMake ✅ (автоматически обнаружен)
- Ninja ⚠️ (рекомендуется установить)
- Visual Studio 2019/2022 (для MSVC компиляции)

## 📁 Структура проекта

```
HuyEngine/
├── build.bat                    # Быстрый запуск (корень)
├── Automation/
│   ├── automation_new.py        # Главный скрипт
│   ├── build_config.py          # Конфигурация
│   ├── build_actions.py         # Действия сборки
│   ├── build_menu.py            # Интерактивное меню
│   └── user_config.template.py  # Шаблон пользовательской конфигурации
├── Engine/                      # Исходный код движка
└── Documentation/               # Документация (на русском)
    ├── BUILD_QUICKSTART.md      # Быстрый старт
    ├── BUILD_SYSTEM_README.md   # Полное руководство
    ├── BUILD_EXAMPLES.md        # Примеры использования
    └── BUILD_FAQ.md             # Часто задаваемые вопросы
```

## ⚙️ Конфигурация

### Поддерживаемые IDE

- **Visual Studio** - использует MSBuild
- **CLion** - использует Ninja/Make с умными папками сборки
- **VSCode** - любая система сборки
- **Manual** - ручная настройка из командной строки

### Системы сборки

- **Ninja** - быстрая параллельная сборка (рекомендуется)
- **MSBuild** - для Visual Studio
- **Unix Makefiles** - кросс-платформенная

### Конфигурации

- **Debug** - отладочная версия с символами
- **Release** - оптимизированная версия
- **RelWithDebInfo** - релиз с информацией для отладки
- **MinSizeRel** - минимальный размер бинарника

### Платформы

- **x64** - 64-битная архитектура (по умолчанию)
- **Win32** - 32-битная архитектура
- **ARM64** - ARM 64-битная архитектура

## 🔧 Пользовательская конфигурация

1. Скопируйте `Automation/user_config.template.py` в `Automation/user_config.py`
2. Отредактируйте пути к инструментам при необходимости
3. Система автоматически загрузит ваши настройки

```python
# Пример user_config.py
def apply_user_config(BuildConfig):
    # Ваши пути к инструментам
    BuildConfig.CMAKE_PATH = r"C:\Tools\cmake\bin\cmake.exe"
    BuildConfig.NINJA_PATH = r"C:\Tools\ninja\ninja.exe"

    # Настройки по умолчанию
    BuildConfig.current_configuration = Configuration.RELEASE
    BuildConfig.verbose = True
```

## 📖 Документация

- **[BUILD_QUICKSTART.md](BUILD_QUICKSTART.md)** - Быстрый старт за 5 минут
- **[Automation/BUILD_SYSTEM_README.md](Automation/BUILD_SYSTEM_README.md)** - Полное руководство
- **[Automation/BUILD_EXAMPLES.md](Automation/BUILD_EXAMPLES.md)** - Примеры для всех сценариев
- **[Automation/BUILD_FAQ.md](Automation/BUILD_FAQ.md)** - Ответы на частые вопросы
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

## ❗ Важно

### Весь код на английском!

Все комментарии в коде, docstrings, сообщения в терминале переведены на английский язык для избежания проблем с
кодировкой и международной совместимости.

### Документация на русском!

Вся пользовательская документация (README, примеры, FAQ) остается на русском языке для удобства.

## ⌨️ Настройка горячих клавиш

### CLion / IntelliJ IDEA

#### Шаг 1: Создание External Tool

1. Откройте **File** → **Settings** (или нажмите `Ctrl+Alt+S`)
2. В левом меню найдите **Tools** → **External Tools**
3. Нажмите кнопку **+** (плюс) для добавления нового инструмента
4. Заполните форму:
    - **Name:** `Build Menu` (или любое другое название)
    - **Description:** `Launch HuyEngine build system` (опционально)
    - **Program:** `$ProjectFileDir$\build.bat`
    - **Arguments:** (оставьте пустым)
    - **Working directory:** `$ProjectFileDir$`
    - **Synchronize files after execution:** ✅ (рекомендуется)
    - **Open console for tool output:** ✅ (обязательно)
    - **Make console active on message in stdout:** ✅ (опционально)
    - **Make console active on message in stderr:** ✅ (опционально)
5. Нажмите **OK**

#### Шаг 2: Назначение горячей клавиши

1. В том же окне **Settings** перейдите в **Keymap**
2. В строке поиска введите: `Build Menu` (или имя, которое вы указали)
3. Разверните **External Tools** → **External Tools** → найдите ваш **Build Menu**
4. Правый клик на **Build Menu** → **Add Keyboard Shortcut**
5. В появившемся окне нажмите вашу комбинацию клавиш:
    - **Рекомендуется:** `Ctrl+Numpad1` (Ctrl + 1 на цифровой клавиатуре)
    - Или: `Ctrl+Alt+B`
    - Или: `Ctrl+Shift+B`
    - Или любая другая свободная комбинация
6. Если комбинация занята, CLion покажет предупреждение - выберите другую или удалите конфликт
7. Нажмите **OK** → **Apply** → **OK**

> 💡 **Совет:** Клавиши Numpad (цифровая клавиатура) удобны для горячих клавиш, так как редко конфликтуют с другими
> командами IDE!

#### Шаг 3: Использование

Теперь в любом файле проекта просто нажмите вашу горячую клавишу (например, `Ctrl+Alt+B`), и откроется меню сборки!

#### Альтернатива: Быстрый запуск без хоткея

Вы также можете запускать через:

- **Tools** → **External Tools** → **Build Menu**
- Или найти через **Search Everywhere** (двойной Shift) → введите "Build Menu"

#### Дополнительные варианты для CLion

**Вариант 2: Создать несколько команд**

Можно создать отдельные External Tools для каждой команды:

1. **Build Menu** → `$ProjectFileDir$\build.bat` → Хоткей: `Ctrl+Numpad1`
2. **Quick Build** → `$ProjectFileDir$\build.bat` с аргументом `build` → Хоткей: `Ctrl+Numpad2`
3. **Quick Run** → `$ProjectFileDir$\build.bat` с аргументом `run` → Хоткей: `Ctrl+Numpad3`
4. **Clean** → `$ProjectFileDir$\build.bat` с аргументом `clean` → Хоткей: `Ctrl+Numpad0`

> 💡 **Удобная схема:** Используйте Numpad клавиши для организации логичной раскладки команд сборки!

**Вариант 3: Использовать Run Configuration**

1. **Run** → **Edit Configurations...**
2. Нажмите **+** → **Shell Script**
3. Заполните:
    - **Name:** `Build System`
    - **Script path:** `C:\DEVOPS\HuyEngine\build.bat`
    - **Working directory:** `$ProjectFileDir$`
4. Теперь можно запускать через `Shift+F10` или назначить свою клавишу

### Visual Studio Code

1. **File** → **Preferences** → **Keyboard Shortcuts** (Ctrl+K Ctrl+S)
2. Нажмите на иконку файла в правом верхнем углу (Open Keyboard Shortcuts JSON)
3. Добавьте в `keybindings.json`:

```json
{
  "key": "ctrl+alt+b",
  "command": "workbench.action.terminal.sendSequence",
  "args": {
    "text": ".\\build.bat\u000D"
  }
}
```

### Visual Studio 2019/2022

1. **Tools** → **External Tools...**
2. Нажмите **Add**:
    - **Title:** `Build Menu`
    - **Command:** `$(ProjectDir)build.bat`
    - **Initial directory:** `$(ProjectDir)`
    - **Use Output window:** ✅
3. **OK** → **Tools** → **Options** → **Environment** → **Keyboard**
4. В поиске введите: `Tools.ExternalCommand1` (номер зависит от позиции)
5. Назначьте горячую клавишу (например: `Ctrl+Alt+B`)

### PowerShell с PSReadLine

Добавьте в профиль PowerShell (`$PROFILE`):

```powershell
# Горячая клавиша Ctrl+B для запуска build.bat
Set-PSReadLineKeyHandler -Chord 'Ctrl+b' -ScriptBlock {
    [Microsoft.PowerShell.PSConsoleReadLine]::RevertLine()
    [Microsoft.PowerShell.PSConsoleReadLine]::Insert('.\build.bat')
    [Microsoft.PowerShell.PSConsoleReadLine]::AcceptLine()
}
```

Применить:

```powershell
# Создать профиль если не существует
if (!(Test-Path $PROFILE))
{
    New-Item -Path $PROFILE -ItemType File -Force
}
# Открыть в редакторе
notepad $PROFILE
```

### Windows Terminal

1. Откройте настройки (Ctrl+,)
2. **Actions** → **Add new action**
3. Добавьте:

```json
{
  "command": {
    "action": "sendInput",
    "input": ".\\build.bat\r"
  },
  "keys": "ctrl+alt+b"
}
```

### Быстрый алиас для PowerShell

Добавьте в `$PROFILE`:

```powershell
# Короткий алиас для build.bat
function bb
{
    .\build.bat
}
Set-Alias -Name build -Value bb
```

Теперь можно запускать просто: `bb` или `build`

## 🐛 Устранение неполадок

### Ошибка кодировки Unicode (UnicodeEncodeError)

**Проблема:** `UnicodeEncodeError: 'charmap' codec can't encode character '\u2713'`

**Решение:** Система сборки автоматически настраивает UTF-8 кодировку для Windows консоли. Если ошибка все равно
возникает:

```powershell
# Вариант 1: Установите кодировку UTF-8 в консоли
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# Вариант 2: Запустите с переменной окружения
$env:PYTHONIOENCODING="utf-8"
python Automation\automation_new.py

# Вариант 3: Добавьте в профиль PowerShell ($PROFILE)
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING="utf-8"
```

**Примечание:** Эта проблема решена в последней версии системы сборки (v2.0.1+).

### build.bat не запускается в PowerShell

```powershell
# Используйте .\ перед именем файла
.\build.bat
```

### CMake не найден

```powershell
# Установите CMake
winget install Kitware.CMake
# или скачайте с https://cmake.org/download/
```

### Ninja не найден

```powershell
# Установите через chocolatey
choco install ninja
# или скачайте с https://github.com/ninja-build/ninja/releases
```

### Visual Studio не обнаружен

Убедитесь что установлен один из:

- Visual Studio 2022 (Community/Professional/Enterprise)
- Visual Studio 2019 (Community/Professional/Enterprise)

## 🎨 Возможности

### ✨ Интерактивное меню

- Цветной вывод в терминале
- Простая навигация
- Отображение текущей конфигурации
- Статус инструментов

### 🔄 Автоматическое обнаружение

- CMake из Program Files и PATH
- Ninja из chocolatey и PATH
- Visual Studio всех версий и изданий
- MSVC компиляторы

### 📦 Умные папки сборки

- `build/` - для Manual/Ninja
- `vs-build/` - для Visual Studio
- `cmake-build-debug/` - для CLion Debug
- `cmake-build-release/` - для CLion Release

### ⚡ Оптимизация

- Параллельная сборка (-j 8)
- Инкрементальная сборка
- Кэширование настроек
- Быстрая очистка

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте [BUILD_FAQ.md](Automation/BUILD_FAQ.md)
2. Проверьте документацию [BUILD_SYSTEM_README.md](Automation/BUILD_SYSTEM_README.md)
3. Посмотрите примеры в [BUILD_EXAMPLES.md](Automation/BUILD_EXAMPLES.md)
4. Используйте `--help` для справки

## 🚀 Следующие шаги

1. Запустите `.\build.bat` чтобы открыть меню
2. Выберите конфигурацию (пункт 1)
3. Выполните Generate (пункт 2 → 1)
4. Выполните Build (пункт 2 → 2)
5. Запустите приложение (пункт 2 → 6)

Готово! 🎉

---

**Версия:** 2.0.1  
**Дата:** 2025-01-13  
**Статус:** ✅ Готово к использованию
**Исправления:** Решена проблема с Unicode символами в Windows консоли

---

<div align="center">
  <em>📝 Документация создана с помощью <strong>GitHub Copilot</strong> 🤖</em>
</div>

