# HuyEngine - Быстрый старт сборки

## 🚀 Быстрый запуск

### Интерактивное меню (рекомендуется для новичков)

```powershell
.\build.bat
```

Или напрямую через Python:

```powershell
python Automation\automation_new.py
```

### Быстрые команды

```powershell
# Генерация проекта
.\build.bat generate

# Сборка
.\build.bat build

# Пересборка
.\build.bat rebuild

# Запуск
.\build.bat run

# Очистка всех папок сборки
.\build.bat clean-all

# Очистка текущей папки сборки
.\build.bat clean
```

## 🎯 Для разных IDE

### Visual Studio

```powershell
# Генерация и открытие в Visual Studio
.\build.bat generate --ide vs --build-system msbuild

# Или через меню: .\build.bat -> Конфигурация -> Visual Studio -> MSBuild
```

### CLion

```powershell
# Генерация для CLion
.\build.bat generate --ide clion --config debug

# CLion автоматически использует cmake-build-* папки
```

### Быстрая сборка (Ninja)

```powershell
# Самый быстрый вариант
.\build.bat rebuild --build-system ninja --config release
```

## 🔧 CMake Presets (альтернативный метод)

Проект поддерживает CMakePresets.json для прямой работы с CMake:

```powershell
# Конфигурация через пресеты
cmake --preset otladka              # Отладка с Ninja
cmake --preset reliz                # Релиз с Ninja
cmake --preset otladka-vs           # Отладка с Visual Studio
cmake --preset reliz-vs             # Релиз с Visual Studio

# Сборка
cmake --build --preset otladka
cmake --build --preset reliz
cmake --build --preset otladka-vs
cmake --build --preset reliz-vs
```

**Доступные пресеты:**

- `otladka` - Отладка (Ninja) → `build/`
- `reliz` - Релиз (Ninja) → `cmake-build-release/`
- `otladka-vs` - Отладка (Visual Studio) → `build-vs/`
- `reliz-vs` - Релиз (Visual Studio) → `cmake-build-release-vs/`

> ⚠️ **Важно**: Для пресетов с Ninja (`otladka`, `reliz`) требуется запуск из **Developer Command Prompt for VS 2022**
> или используйте `.\build.bat`, который автоматически настраивает окружение!

## 📖 Полная документация

Смотрите `Automation/BUILD_SYSTEM_README.md` для подробной документации.

## ⌨️ Справка

```powershell
.\build.bat --help
python Automation\automation_new.py --help
```

## 🔧 Структура команд

```
.\build.bat [действие] [опции]

Действия:
  generate    - Генерация проекта
  build       - Сборка
  rebuild     - Пересборка
  clean       - Очистка
  clean-all   - Очистка всех папок
  run         - Запуск
  format      - Форматирование кода
  menu        - Меню (по умолчанию)

Опции:
  -i, --ide           IDE (vs, clion, vscode, manual)
  -b, --build-system  Система сборки (ninja, msbuild, make)
  -c, --config        Конфигурация (debug, release, etc.)
  -p, --platform      Платформа (x64, win32, arm64)
  --verbose           Подробный вывод
  --no-clean          Не очищать перед сборкой
```

## 🆕 Что нового

- ✅ Интерактивное меню с цветным выводом
- ✅ Поддержка Visual Studio, CLion, VSCode
- ✅ Выбор системы сборки (Ninja, MSBuild, Make)
- ✅ Отдельные папки для каждой конфигурации
- ✅ Автоматическое обнаружение инструментов
- ✅ Удобное управление всеми папками сборки
- ✅ Полная документация на русском

---

**Версия системы сборки**: 2.0  
**Дата**: 2025-11-13  
**Обновлено**: Добавлена поддержка CMakePresets.json

---

<div align="center">
  <em>📝 Документация создана с помощью <strong>GitHub Copilot</strong> 🤖</em>
</div>

