# Системы сборки HuyEngine

## 🔧 Доступные системы сборки

### 1. Ninja (рекомендуется)

**Быстрая кросс-платформенная система сборки**

- ✅ Самая быстрая сборка
- ✅ Используется по умолчанию
- ⚠️ Требует Developer Command Prompt для MSVC

```powershell
.\build.bat generate --build-system ninja
.\build.bat build
```

### 2. MSBuild (Visual Studio)

**Родная система сборки Visual Studio**

- ✅ Полная интеграция с Visual Studio
- ✅ Открывает проект в VS
- ✅ Подходит для GUI разработки

```powershell
.\build.bat generate --build-system msbuild --ide vs
```

### 3. Unix Makefiles

**Классические Makefiles**

- ✅ Универсальная система
- ⚠️ Медленнее чем Ninja

```powershell
.\build.bat generate --build-system make
```

## 📋 Как переключить систему сборки

### Способ 1: Через build.bat (рекомендуется)

```powershell
# Переключение на Ninja
.\build.bat generate --build-system ninja

# Переключение на MSBuild
.\build.bat generate --build-system msbuild --ide vs

# Переключение на Make
.\build.bat generate --build-system make
```

### Способ 2: Через интерактивное меню

```powershell
.\build.bat
# Выберите: 1 → Configuration → Build System → выберите систему
```

### Способ 3: Через CMake Presets

```powershell
# Ninja
cmake --preset otladka
cmake --build --preset otladka

# Visual Studio (MSBuild)
cmake --preset otladka-vs
cmake --build --preset otladka-vs
```

## 🎯 Для разных сценариев

### Разработка в CLion

```powershell
.\build.bat generate --ide clion --build-system ninja
# CLion автоматически использует Ninja
```

### Разработка в Visual Studio

```powershell
.\build.bat generate --ide vs --build-system msbuild
# Откроется Visual Studio с проектом
```

### Быстрая сборка из командной строки

```powershell
.\build.bat rebuild --build-system ninja --config release
```

## 📁 Папки сборки для разных систем

Каждая система сборки использует свою папку:

- **Ninja Debug**: `build-msvc/`
- **Ninja Release**: `build-msvc/` (с другой конфигурацией)
- **MSBuild**: `build-vs/`
- **Make**: `build-make/`

Для очистки всех папок:

```powershell
.\build.bat clean-all
```

## ⚡ Сравнение производительности

| Система | Скорость | Интеграция VS | Кроссплатформа |
|---------|----------|---------------|----------------|
| Ninja   | ⭐⭐⭐⭐⭐    | ⭐⭐⭐           | ⭐⭐⭐⭐⭐          |
| MSBuild | ⭐⭐⭐      | ⭐⭐⭐⭐⭐         | ⭐              |
| Make    | ⭐⭐       | ⭐⭐            | ⭐⭐⭐⭐⭐          |

## 🔍 Проверка текущей системы

```powershell
.\build.bat
# Смотрите "Build System:" в Current configuration
```

## 💡 Рекомендации

1. **Для повседневной разработки**: Используйте **Ninja** для быстрой сборки
2. **Для работы в Visual Studio**: Используйте **MSBuild** для полной интеграции
3. **Для CI/CD**: Используйте **Ninja** для скорости

---

**Совет**: Вы можете иметь несколько папок сборки одновременно для разных систем!

---

<div align="center">
  <em>📝 Документация создана с помощью <strong>GitHub Copilot</strong> 🤖</em>
</div>

